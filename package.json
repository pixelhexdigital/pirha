{
  "name": "pirha",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm install --prefix client vite && npm run build --workspace=client && chmod -R 755 client/dist",
    "start": "npm install --prefix server nodemon && npm run start --workspace=server"
  }
}
