{
  "name": "pirha",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn build --workspace=client",
    "start": "yarn start --workspace=server"
  }
}
